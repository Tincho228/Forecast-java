package com.example.view;

public class WeatherView {
    public void showWeather(String response) {
        System.out.println(response);
        // 3. Show the response in the console, order with jsp or something similar
        // 4. The hosting is left for the end
        // 5. Ineract with main to obtain the lat and lon
    }
}
